name: template-smart-defaults
skill: template-smart-defaults

defaults:
  graders:
    - type: rubric
      rubric:
        - Executes the skill's intended workflow rather than giving generic advice.
        - Produces evidence-backed, actionable results.
        - Reports validation failures truthfully.

stimuli:
  - prompt: "Choose sensible defaults for a dotnet template from project context."
    graders:
      - type: rubric
        rubric:
          - Satisfies the requested task using the skill workflow.
          - Does not invent unavailable project facts.
          - Includes observable validation or evidence.
  - prompt: "Infer framework and project options without inventing unsupported values."
    graders:
      - type: rubric
        rubric:
          - Satisfies the requested task using the skill workflow.
          - Does not invent unavailable project facts.
          - Includes observable validation or evidence.
  - prompt: "Apply defaults while preserving explicit user parameters."
    graders:
      - type: rubric
        rubric:
          - Satisfies the requested task using the skill workflow.
          - Does not invent unavailable project facts.
          - Includes observable validation or evidence.
  - prompt: "Explain why a smart default was selected."
    graders:
      - type: rubric
        rubric:
          - Satisfies the requested task using the skill workflow.
          - Does not invent unavailable project facts.
          - Includes observable validation or evidence.
  - prompt: "Detect when no safe default exists and ask for the missing choice."
    graders:
      - type: rubric
        rubric:
          - Satisfies the requested task using the skill workflow.
          - Does not invent unavailable project facts.
          - Includes observable validation or evidence.