name: template-authoring
skill: template-authoring

defaults:
  graders:
    - type: rubric
      rubric:
        - Executes the skill's intended workflow rather than giving generic advice.
        - Produces evidence-backed, actionable results.
        - Reports validation failures truthfully.

stimuli:
  - prompt: "Author a dotnet template with correct symbols and conditional content."
    graders:
      - type: rubric
        rubric:
          - Satisfies the requested task using the skill workflow.
          - Does not invent unavailable project facts.
          - Includes observable validation or evidence.
  - prompt: "Add a new parameter to an existing template without breaking existing invocations."
    graders:
      - type: rubric
        rubric:
          - Satisfies the requested task using the skill workflow.
          - Does not invent unavailable project facts.
          - Includes observable validation or evidence.
  - prompt: "Review a template.json and identify invalid or ambiguous configuration."
    graders:
      - type: rubric
        rubric:
          - Satisfies the requested task using the skill workflow.
          - Does not invent unavailable project facts.
          - Includes observable validation or evidence.
  - prompt: "Create a template README from the repository conventions."
    graders:
      - type: rubric
        rubric:
          - Satisfies the requested task using the skill workflow.
          - Does not invent unavailable project facts.
          - Includes observable validation or evidence.
  - prompt: "Refactor duplicated template metadata into reusable guidance."
    graders:
      - type: rubric
        rubric:
          - Satisfies the requested task using the skill workflow.
          - Does not invent unavailable project facts.
          - Includes observable validation or evidence.