# Workflows

Workflows are dynamic orchestration artifacts and may be generated from saved runs. Do not scaffold an executable workflow unless the target runtime contract is known.

Template: `../assets/workflow.template` is intentionally a non-executable starting point.