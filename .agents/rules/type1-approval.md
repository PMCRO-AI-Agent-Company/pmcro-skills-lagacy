# TYPE1 mutations require approval

Mirrors `laws.md`. Any state-changing action (git operations,
deleting files, rewriting `.pmcro/queue.jsonl` outside the normal
claim/enqueue protocol) requires explicit human approval before
execution, even when the Orchestrator/Maker judges it safe.
