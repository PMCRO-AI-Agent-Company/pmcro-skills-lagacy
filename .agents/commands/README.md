# Project Commands

This directory is reserved for repository-local authoring and maintenance commands.
Keep distributable plugin commands, when supported, inside the plugin boundary; do not move project commands into plugins.
