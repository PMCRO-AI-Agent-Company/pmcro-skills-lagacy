refactor: drop colony-laws.md filename, rename agentskills plugins to pmcro-*

Renames colony-laws.md to laws.md and fixes 12 live references to the
old filename (broader "colony" wording elsewhere is intentionally left
alone for now -- it's used as load-bearing terminology in places, not
just flavor text, so a blind sweep risked losing meaning; see the
sealed trail for the reasoning).

Renames plugins/agentskills -> plugins/pmcro-skill-creator and
plugins/agentskills-template-engine -> plugins/pmcro-template-engine
(directories, plugin.json name fields x6, all 4 marketplace.json
copies + plugins.lock.json + capability-registry.json + the handful of
docs that named them). Both are breaking renames for anyone invoking
/agentskills:<skill> or /agentskills-template-engine:<skill> -- the new
namespaces are /pmcro-skill-creator:<skill> and
/pmcro-template-engine:<skill>. Left untouched: the unrelated .NET
solution's own AgentSkills.* C# namespace, the genuine external
agentskills.io spec citations in eng/skill-validator, and a couple of
generic "Agent Skills ecosystem" mentions inside pmcro-skill-creator
that are ambiguous rather than clearly this plugin's own identity (also
flagged in the trail).

Bonus fix: .github/plugin/marketplace.json was missing the pmcro and
pmcro-skills plugin entries the other 3 marketplace copies already had
-- added both while already touching every copy for the renames above.

Captured via proper intake (Add-PmcroIntake -> Resolve-PmcroIntake
split into 4 derived queue items) rather than acted on directly, per
this session's own earlier correction about messy vs. canonical seed
intent. Full detail in trail
.pmcro/trails/cycle-20260903-194620-task-drop-colony-and-rename-plugins.md.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01RMd8AuXcayFvpNqch9J78W
