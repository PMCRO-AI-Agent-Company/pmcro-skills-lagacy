# Trail: cycle-20260903-185608-task-intake-20260903-135107390

trail_id: cycle-20260903-185608-task-intake-20260903-135107390
task_id: task-intake-20260903-135107390
domain: pmcro-governance
priority: 2
opened: 2026-09-03
run_id: task-intake-20260903-135107390
checkpoint_ref: (cleared on completion)
trail_sealed: true

## Seed intent

(See the resolved intake item's own `seed_intent`, produced in the prior
cycle -- cycle-20260903-185107-task-intake-seed-attribution-correction --
and not re-quoted in full here.) In short: audit and correct the queue
items filed this session that used raw/paraphrased human dictation
directly as `seed_intent` instead of the properly refined product, with
the original wording preserved separately in `messy_seed_text` per
`seed-intent-contract.md`. Human follow-up: "yes continue," confirming
standing autonomy to proceed without pausing to ask at each step.

## OrchestratorFrame

Claimed via `Claim-PmcroTask` (auto-selected by lowest priority number --
this item's priority 2 was in fact the highest-priority open item in the
queue at claim time, so no manual targeting was needed). Lease/checkpoint
established per the existing Run/Lease mechanism, no ad hoc bypass.

Re-audited the "5 items" scope named in the seed_intent before touching
anything, rather than fixing all 5 blindly:
- `task-git-lifecycle-plugin`, `task-desktop-commander-plugin`,
  `task-anthropic-agent-design-pattern-mapping` -- genuinely
  non-compliant. All three trace to one combined raw dictated message
  (git-plugin + Desktop-Commander-plugin + Anthropic-pattern-mapping +
  the "always execute inside a cycle" law + the AgentsRuntime naming
  decision, all in one dictation). Their existing `seed_intent` text was
  already a reasonable paraphrase/refinement of the relevant slice, not
  verbatim dictation, but was wrapped in a "Human instruction 2026-09-03:"
  prefix and had no `messy_seed_text` preserved.
- `task-adopt-command-asset-and-precondition-footer` -- also genuinely
  non-compliant, same pattern, traced to its own distinct raw dictated
  message (the pmcro-skills_archive "skill command" question) and a
  garbled "2026-08-26_archive" fragment in its own prefix (an artifact
  from this agent's own earlier drafting, not the human's wording).
- `task-save-pmcroqueue-reformats-whole-file` -- audited and found
  **compliant, not a violation**: this item was `created_by: "reflector"`
  from the start, synthesized directly from a diff this agent read, with
  no underlying raw human message to preserve. The earlier trail's count
  of "5 items" overstated the actual scope by one; corrected here rather
  than silently fixing something that didn't need it. Left untouched.

Also checked whether the earlier finding's secondary recommendation
("harden enqueue.ps1 against raw-looking SeedIntent text") was still
warranted: read `plugins/pmcro-loop/skills/queue-enqueue/SKILL.md`'s
frontmatter description directly -- it already states, verbatim, "For a
raw, not-yet-classified message use intake-message.ps1 instead." The
documentation was never the gap; six items were filed in violation of
already-clear, already-visible guidance purely because this agent didn't
follow it. Recorded that conclusion plainly in Reflection rather than
adding brittle content-sniffing validation to `Add-PmcroQueueItem` to
paper over a discipline failure with a heuristic that would be easy to
defeat and hard to get right (e.g. matching on a "Human instruction:"
prefix that a future caller simply wouldn't use).

## MakeFrame

For the 4 genuinely non-compliant items: loaded the queue via
`Get-PmcroQueue`, for each item set `messy_seed_text` to the actual
original raw dictated text (pulled from this session's own conversation
record, not reconstructed or paraphrased) and rewrote `seed_intent` to
drop the "Human instruction ...:" provenance-prefix framing (provenance
now lives in `created_by` + `messy_seed_text`, not embedded in the intent
text itself) while preserving the already-refined actionable content
essentially verbatim -- this was not a rewrite of what those items ask
for, only a schema-compliance correction. Saved via `Save-PmcroQueue`
(the engine's own function, not raw text splicing).

`task-save-pmcroqueue-reformats-whole-file`: no change made; audited and
confirmed compliant.

Closed the Run: `Complete-PmcroRun -FinalStatus done` -- sets `status:
done`, clears `lease_owner`/`heartbeat_at`/`lease_expires_at`/
`checkpoint_ref`, deletes the checkpoint file. Did not hand-set `status`
before calling it (an earlier draft of the completion script did this
redundantly; caught by reading `Complete-PmcroRun`'s actual implementation
before running, not by trial and error against the live queue).

## CheckFrame
verdict: pass

- `queue.jsonl`: 39 lines both before and after (no items added/removed,
  only 4 mutated in place), every line parses as valid JSON (verified
  line-by-line via `ConvertFrom-Json`, not just spot-checked).
- All 4 fixed items confirmed to carry a non-empty `messy_seed_text` and
  a `seed_intent` free of the old provenance-prefix wrapper (verified via
  a `Format-Table` preview read back from the saved file, not just
  trusted from the write call).
- `task-save-pmcroqueue-reformats-whole-file` confirmed untouched
  (deliberately excluded, not silently skipped).
- `task-intake-20260903-135107390` confirmed `status: done`, checkpoint
  file removed, lease fields absent from the final read-back.
- Scratch driver scripts (`run-claim-task.ps1`,
  `run-fix-seed-intent-audit.ps1`, `run-verify-and-complete.ps1`) removed
  post-run.

## Reflection
outcome: done
next_seed_intent: none required from this cycle. The one open question
this cycle resolved rather than deferred: whether `enqueue.ps1` needs a
code-level guard against raw-dictation `-SeedIntent` text. Conclusion:
no -- the skill's own SKILL.md frontmatter already carries that guidance
clearly ("For a raw, not-yet-classified message use intake-message.ps1
instead"); the defect was a discipline lapse (repeated 6 times this
session, including once even after building `enqueue.ps1` itself), not a
missing or unclear contract. This cycle's own use of
`intake-message.ps1` -> `resolve-intake.ps1` for the correction that
prompted it, and again here treating the human's "yes continue" as intake
context rather than a fresh unscoped instruction, is the actual
behavioral fix -- demonstrated in practice across two consecutive cycles
now, not merely documented.
