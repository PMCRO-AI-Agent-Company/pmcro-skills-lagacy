# Trail: cycle-20260903-143635-task-reconcile-project-vs-projects-topology

trail_id: cycle-20260903-143635-task-reconcile-project-vs-projects-topology
task_id: task-reconcile-project-vs-projects-topology
domain: pmcro-governance
priority: 2
opened: 2026-09-03
engine_generated: true

## Seed intent
Human question 2026-09-03: "projects actually suppose to be deleted and Should they go within the root?" -- investigated and confirmed this points at a real, unresolved structural debt, not a stray folder (task-repo-cleanup, already closed, was only ever about one specific candidate folder _pmcro-duplicate-backup-20260902 which never existed anywhere -- unrelated to this). Findings: (1) project/ (singular) at true repo root is an INTENTIONAL, documented template/example (LAYOUTS.md "Project (project/ -> repo root)" section; created by commit 9964a45 "Restructure agent-skills to plugin-catalog convention... add project/") demonstrating what a consumer repo's OWN root should contain -- its .pmcro/ is a frozen, single-cycle illustrative fixture (task-understand-pmcro-framework, 2026-09-02, untouched since commit f3d3d62) and should NOT be deleted or treated as stale cruft. (2) projects/pmcro-skills/ (plural, confusingly named the same as the outer repo) is where ALL real live work actually happens -- the .NET solution (AgentSkills.slnx, src/, workflows/, tests/) and the live PMCR-O colony state (.pmcro/, colony-laws.md, CONTEXT.md, INSTRUCTIONS.md) this entire session has operated from, first introduced by a different commit (7fbbbbc, "Implement PMCR-O Clean Architecture CQRS slice"). (3) The smoking gun: CONTEXT.md (projects/pmcro-skills/CONTEXT.md) states colony-laws.md "is intentionally kept at repository root" and describes "P:\agent-skills\.claude-plugin\marketplace.json at the agent-skills monorepo root" and "P:\agent-skills\plugins\pmcro-loop\" as a "top-level monorepo plugin, sibling to agentskills and agent-design-patterns" -- none of which matches the actual current filesystem (there is no P:\agent-skills; everything, including plugins/pmcro-loop, actually lives under P:\source\pmcro-skills, the same repo as projects/pmcro-skills/ itself). CONTEXT.md was written for an earlier topology (this project/pmcro-skills sub-project nested inside a separate, larger P:\agent-skills monorepo) that no longer exists -- the repos appear to have been consolidated/renamed at some point without CONTEXT.md being updated to match. Scope for whoever claims this: (a) confirm via git history exactly when/how the P:\agent-skills monorepo and this repo became one, to avoid guessing further than this cycle already has; (b) decide and get explicit human approval (TYPE1 -- this is exactly what colony-laws.md's approval protocol requires for a destructive/structural repo mutation) on whether to migrate projects/pmcro-skills/*'s live content (.pmcro/, colony-laws.md, CONTEXT.md, INSTRUCTIONS.md, src/, tests/, docs/, eng/, workflows/, artifacts/) up to the true repo root, collapsing the now-apparently-redundant nesting, OR to instead just fix CONTEXT.md's stale claims to accurately describe the nested structure as intentional and permanent; (c) do NOT touch project/ (singular) as part of this -- it is a separate, deliberate template/example, unrelated to this nesting question; (d) if migration is approved, this touches live .pmcro/ state (queue.jsonl, trails/, session-state.md, constraints/) that must not be lost or duplicated mid-move, the real .NET solution under src/ (project references, .sln/.slnx paths), and every script/doc that hardcodes a projects/pmcro-skills/ path prefix (queue-enqueue's own references already had one such bug found and fixed earlier this session in capability-gap-and-composition.md -- grep for the literal string across the whole repo before starting, there are likely more). This is exactly the kind of change docs/architecture-governance.md's own Change boundaries and Governance boundaries sections describe -- Planner must scope the boundary and acceptance evidence, Maker executes only the approved scope, Checker independently validates, before any file is moved.

**Follow-up seed intent (same day, explicit human override of this cycle's own (b) recommendation):** "Yes delete projects and project folder as well as LAYOUT file and create .pmcro within the root CONTEXT.md and INSTRUCTIONS.md within .pmcro or .agent theres need yo be a config file that has entiere project directority configurecton etc." The human heard the case for preserving `project/` (singular) as an intentional template and explicitly decided otherwise -- both `project/` and `projects/` were to be deleted, not just `projects/`, `LAYOUTS.md` was to go with it, `.pmcro/` was to move to the true root, `CONTEXT.md`/`INSTRUCTIONS.md` were to land under `.agents/` (choice between `.pmcro/` and `.agents/` explicitly left to this cycle's judgment -- `.agents/` was chosen since these are narrative/governance docs, not machine-read runtime state), and a new directory-configuration doc was requested to prevent the exact CONTEXT.md-staleness problem this cycle's own investigation surfaced. Executed under the human's standing "autonomous, do as you recommend" grant from earlier the same session, with each stage verified before proceeding rather than run blind.

## PlanFrame (Planner)
Scope: (1) delete `project/` (singular) and `projects/` (plural) entirely; (2) delete `LAYOUTS.md`; (3) relocate `.pmcro/`, `colony-laws.md`, `trail-format.md`, and all of `projects/pmcro-skills/`'s other true-root-shaped content (`.codex-plugin`, `.config`, `docs`, `src`, `workflows`, loose config files, `engine/`) to the true repo root; (4) move `CONTEXT.md`/`INSTRUCTIONS.md` to `.agents/`; (5) merge, not silently overwrite, everywhere a nested path collided with something already at true root (8 real file-level collisions found via a corrected diff -- an earlier collision-detection script under-reported due to inconsistent path-prefix stripping between its two comparison lists, caught and fixed before any file was moved on the wrong data); (6) union-merge `.gitignore`/`.gitattributes` and content-merge `AGENTS.md` (both sides had genuinely non-overlapping, valid content) rather than picking a single winner; (7) author the requested directory-configuration doc; (8) fix every live (non-sealed-trail) reference to the old `projects/pmcro-skills/` path prefix or the fictitious `P:\agent-skills` root; (9) verify via the existing Pester suite and a live `Find-PmcroRoot` resolution check before calling this done.

## MakeFrame (Maker)
Executed in four scripted stages (`git mv` for clean 1:1 renames, `Copy-Item`+`Remove-Item` fallback where `git mv` hit a transient Windows file-lock "Permission denied" on `projects/pmcro-skills/engine/` -- resolved without killing any process, `git add -A` afterward let git's own similarity detection recognize the copy+delete pairs as renames):

1. Clean 1:1 moves (no collision): `.codex-plugin`, `.config`, `docs`, `src`, `workflows`, `engine/` (via the copy fallback), plus loose root files (`.dockerignore`, `.markdownlint-cli2.jsonc`, `AgentSkills.slnx`, `CODE_OF_CONDUCT.md`, `CONTRIBUTING.md`, `Directory.Build.{props,targets}`, `Directory.Packages.props`, `global.json`, `LICENSE`, `plugin.json`, `SECURITY.md`, `trail-format.md`, `version.json`), `.pmcro/`, `colony-laws.md`, and `CONTEXT.md`/`INSTRUCTIONS.md` -> `.agents/`.
2. 8 file-level collisions resolved by content/recency, not by position: **root won** (nested was a stale local copy, root was this session's actively-edited version) for `.agents/plugins/marketplace.json`, `.agents/plugins/plugins.lock.json`, `.claude-plugin/marketplace.json`, `.cursor-plugin/marketplace.json`, `.agents/skills/create-skill/SKILL.md`; **nested won** (nested was materially larger/more recent) for `.agents/skills/create-custom-agent/SKILL.md`, `eng/eval-quality/check_eval_quality.py`, `eng/eval-quality/README.md`.
3. Bulk-merged the remaining ~410 non-colliding files under `.agents/`, `.claude-plugin/` (`plugin.json`), `.cursor-plugin/`, `.github/`, `eng/` (including the full `skill-validator` C# solution), `tests/` via directory-tree copy then removal of the now-empty nested source, staged with `git add -A` (git's rename detection picked these up as renames from their content match, confirmed in the resulting status: 383 R / 13 D / 10 A / 8 M / 1 untracked-scratch of 448 total entries at that checkpoint).
4. Loose-file collisions: `.gitignore` and `.gitattributes` union-merged (all rules from both sides are additive, no conflicts); `AGENTS.md` content-merged (root's marketplace-wide conventions kept as the base, nested's PMCR-O-specific Architecture/Skill-invocation/Lifecycle/Memory/Execution-architecture/Commands/Mutation sections appended under a new "PMCR-O project conventions" heading, annotated as pre-consolidation content that still applies -- this was a genuine judgment call since the two files did not actually conflict in substance, just in framing); `README.md` root won outright (57 substantive lines on the real packaging/projection architecture vs. 23 generic lines) but its "Plugins" section referenced the now-deleted `projects/pmcro-skills` path and was rewritten as a "Repository layout" section pointing at the true-root paths and the new topology doc.
5. Removed `LAYOUTS.md`, `project/` (singular, `git rm -r`), and the now-fully-emptied `projects/` wrapper (including its untracked `.NET` build cruft under `artifacts/`, confirmed via `git ls-files` to hold zero tracked content, and a stray `.gitkeep`).
6. A genuine structural gap surfaced while fixing path references: the marketplace manifests' `pmcro-skills` plugin entry pointed `"source"` at the now-deleted `./projects/pmcro-skills`, and the real skill implementations that selector should resolve to live at `.agents/skills/` -- but `.agents/` had no `plugin.json`, so it was not actually a structurally valid plugin source. Rather than leave a known-broken reference, created `.agents/plugin.json` and `.agents/version.json` (minimal schema, matching the smallest existing plugin `agent-design-patterns`) and repointed all 4 marketplace/lock `"source"` fields to `./.agents`.
7. Rewrote `.agents/CONTEXT.md` to remove the disproven `P:\agent-skills` monorepo claims and describe the true post-consolidation topology, cross-referencing the new doc.
8. Authored `.pmcro/repo-topology.md` -- the requested "entire project directory configuration" file: documents the 2026-09-03 consolidation history, a full top-level directory map with one-line purpose per entry, the plugin-registration convention, and an explicit "what no longer exists, do not recreate" section naming `projects/`, `project/`, `LAYOUTS.md`, and any `P:\agent-skills` path.
9. Grepped the whole repo (excluding sealed trails, `.jsonl` queue data, and this cycle's own scratch scripts) for `projects/pmcro-skills` and `P:\agent-skills`; found and fixed 7 live hits beyond the manifests already covered in step 6: `plugins/pester-skills/skills/run-tests/references/pester-conventions.md`, `plugins/pmcro-loop/references/common-pitfalls.md` (annotated as historical, file removed), `plugins/pmcro-loop/scripts/validate-skill.ps1` (comment annotated similarly), `plugins/pmcro/skills/foundation/references/capability-gap-and-composition.md`, `plugins/pmcro/skills/source-dump/references/source-dump.md` (2 lines). Final grep against live content returns zero unannotated hits.
10. Deleted all scratch driver scripts (`run-migrate-*.ps1`, `run-inventory-collisions*.ps1`, `run-diff-remaining-collisions.ps1`, `run-check-*.ps1`, `run-fix-*.ps1`, `run-verify-1.ps1`, `run-finalize-1.ps1`) per this session's standing discipline of not committing scratch.

## CheckFrame (Checker)
verdict: pass

- All 6 edited/created JSON manifests (`.agents/plugins/marketplace.json`, `.agents/plugins/plugins.lock.json`, `.claude-plugin/marketplace.json`, `.cursor-plugin/marketplace.json`, `.agents/plugin.json`, `.agents/version.json`) parsed cleanly via `ConvertFrom-Json`, not just visually inspected.
- `Find-PmcroRoot` (the upward-search helper already made robust across all 12 `pmcro-loop` wrapper scripts in an earlier cycle this session) live-resolves to the new true-root `.pmcro/` from three independent starting points: repo root, `.agents/skills`, `plugins/pmcro-loop` -- confirming the relocation is actually transparent to tooling, not just structurally present.
- The full existing Pester suite (`tests/pmcro-loop/approve-operation`, `tests/pmcro-loop/queue-enqueue`) re-run from the new post-migration location: **8/8 passed**, 0 failed, 1.45s. No test needed modification.
- Repo-wide `git grep` for `projects/pmcro-skills` and `P:\\agent-skills` (excluding sealed trails and queue data, which are historical record and correctly left untouched) returns zero hits outside the deliberately-annotated historical mentions in `AGENTS.md`, `.agents/CONTEXT.md`, `.pmcro/repo-topology.md`, and the two annotated `pmcro-loop` files.
- `git status --short` after the full merge shows the expected shape: the ~410-file bulk merge registered as renames (git's own content-similarity detection), not as spurious delete+add pairs that would have obscured history/blame.
- Not independently re-verified this cycle: a `dotnet build`/`dotnet test AgentSkills.slnx` pass on the relocated .NET solution (`src/`, `tests/AgentSkills.Workflows.*`) -- the file moves were plain renames with no path-relative project-reference edits needed (`.slnx`/`.csproj` references are already relative and moved as a unit), but a build was not run live this cycle to confirm; flagged as a gap below rather than silently assumed.

## Reflection (Reflector)
outcome: done

The structural debt this task was originally filed to investigate is resolved: `project/` (singular) and `projects/` (plural) no longer exist, the live colony (`.pmcro/`, `colony-laws.md`, `.agents/`) and the .NET solution both live at the true repo root, `CONTEXT.md` no longer makes disproven claims about a separate monorepo, and `.pmcro/repo-topology.md` now exists specifically to keep that from recurring silently. The original task's own recommendation to leave `project/` alone was explicitly and knowingly overridden by the human after hearing the reasoning -- recorded here rather than silently discarded, since a future reader should be able to see that this wasn't an oversight.

next_seed_intent: Two follow-ups worth queuing, not fabricated as urgent but real: (1) run `dotnet test AgentSkills.slnx` from the true root at least once to close the one CheckFrame gap noted above -- low risk (pure file moves) but not yet actually exercised; (2) the `.agents/plugin.json` created this cycle to make the `pmcro-skills` marketplace entry structurally valid again is a first-cut minimal manifest (matching `agent-design-patterns`' shape) -- worth a follow-up look at whether it should carry a `repository` field or other metadata the way `plugins/pmcro-loop/plugin.json` does, and whether `.agents/` being simultaneously "this repo's own agent workspace" and "an installable marketplace plugin source" is the shape this project actually wants long-term, or whether the marketplace entry should instead be retired now that `pmcro-skills` skills are indistinguishable from the repo's own root tooling.

trail_sealed: true
